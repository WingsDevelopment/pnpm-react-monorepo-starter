import { Button } from "shared-ui";

function App() {
  return (
    <div className="App">
      <Button onClick={() => console.log("clicked")}>Click me</Button>
    </div>
  );
}

export default App;
